package com.example.glassesrelay.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette: deep blue → cyan accent ──
val BluePrimary = Color(0xFF3D5AFE)       // Vibrant indigo-blue
val BluePrimaryDark = Color(0xFF0039CB)
val CyanAccent = Color(0xFF00E5FF)        // Electric cyan
val CyanAccentMuted = Color(0xFF18FFFF)

// ── Surface tones (dark mode) ──
val SurfaceDark = Color(0xFF0F1318)       // Near-black
val SurfaceContainer = Color(0xFF1A1F27)  // Card backgrounds
val SurfaceContainerHigh = Color(0xFF232830)
val SurfaceContainerHighest = Color(0xFF2C323B)

// ── On-surface ──
val OnSurfaceLight = Color(0xFFE2E8F0)
val OnSurfaceMuted = Color(0xFF94A3B8)
val OnSurfaceDim = Color(0xFF64748B)

// ── Status colours ──
val StatusGreen = Color(0xFF4ADE80)
val StatusAmber = Color(0xFFFBBF24)
val StatusRed = Color(0xFFF87171)

// ── Error / tertiary ──
val ErrorPink = Color(0xFFFF5370)
val TertiaryViolet = Color(0xFFA78BFA)